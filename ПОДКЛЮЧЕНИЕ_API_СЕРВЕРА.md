# 🔌 Как подключить API сервер к сайту

## 🎯 Быстрое решение проблемы

Ваш сайт не показывает данные, потому что файлы `services/ephemeris.js` и `services/lunarDays.js` не загружены на GitHub.

**Сначала загрузите эти файлы!** (см. файл `КАК_ЗАГРУЗИТЬ_ФАЙЛЫ.txt`)

---

## 📡 Варианты подключения API

### Вариант 1: Использовать существующий NASA API (УЖЕ РАБОТАЕТ!)

**Не нужно ничего делать!** NASA/USNO API уже подключен и работает.

Просто загрузите файлы на GitHub, и всё заработает.

---

### Вариант 2: Создать свой API сервер (для Swiss Ephemeris)

Если хотите использовать Swiss Ephemeris для максимальной точности:

#### Шаг 1: Установите Node.js

1. Скачайте: https://nodejs.org/
2. Установите (отметьте "Add to PATH")
3. Проверьте: откройте PowerShell и выполните:
   ```bash
   node --version
   ```

#### Шаг 2: Запустите API сервер

1. Откройте PowerShell в папке `server`
2. Выполните:
   ```bash
   npm install
   npm start
   ```
3. Сервер запустится на `http://localhost:3000`

#### Шаг 3: Подключите к сайту

1. Откройте `services/ephemeris.js`
2. Найдите строку:
   ```javascript
   customApiUrl: null,
   ```
3. Замените на:
   ```javascript
   customApiUrl: 'http://localhost:3000/api/ephemeris',
   ```
4. Сохраните и загрузите на GitHub

#### Шаг 4: Разместите API на хостинге

Для продакшена разместите API на:
- **Render** (бесплатно): https://render.com
- **Railway** (бесплатно): https://railway.app
- **Vercel** (бесплатно): https://vercel.com
- **Heroku** (платно): https://heroku.com

После размещения замените URL в `customApiUrl`.

---

### Вариант 3: Использовать готовый API (самый простой)

Можно использовать готовые бесплатные API:

1. **USNO API** (уже подключен) - бесплатно, без ключа
2. **NASA API** - бесплатно, нужен ключ (https://api.nasa.gov)

---

## 🚀 Быстрый старт (рекомендуется)

### Сейчас сделайте это:

1. **Загрузите файлы на GitHub:**
   - `services/ephemeris.js`
   - `services/lunarDays.js`
   - Обновленный `app.js`

2. **Проверьте сайт:**
   - Откройте: https://animen398-glitch.github.io/lunar-portal/
   - Откройте консоль (F12)
   - Должны увидеть: `✅ Данные получены`

3. **Если всё работает:**
   - NASA API уже подключен!
   - Данные реальные и точные
   - Ничего дополнительно делать не нужно

---

## 📝 Что делает API сервер

API сервер (`server/server.js`):
- Принимает запросы с датой и координатами
- Получает данные от NASA/USNO API
- Возвращает данные в нужном формате
- Может быть расширен для Swiss Ephemeris

---

## 🔧 Интеграция Swiss Ephemeris (продвинутый уровень)

Если хотите использовать Swiss Ephemeris:

1. **Скачайте Swiss Ephemeris:**
   - https://github.com/arturania/swisseph
   - Или с официального сайта: https://www.astro.com/swisseph/

2. **Установите зависимости:**
   - Для Node.js: нужны биндинги (swisseph-node)
   - Для Python: pip install pyswisseph
   - Для других языков: см. документацию

3. **Модифицируйте `server.js`:**
   - Добавьте вызовы Swiss Ephemeris
   - Замените упрощенные расчеты на точные

4. **Разместите на хостинге:**
   - Загрузите код на Render/Railway/Vercel
   - Получите URL API
   - Обновите `customApiUrl` в `ephemeris.js`

---

## ✅ Итог

**Для начала:**
1. Загрузите файлы `services/` на GitHub
2. Всё заработает с NASA API

**Для продвижения:**
1. Создайте API сервер (файлы в папке `server/`)
2. Разместите на хостинге
3. Подключите к сайту

**Для максимальной точности:**
1. Интегрируйте Swiss Ephemeris в API сервер
2. Используйте для расчетов

---

**Вопросы?** Откройте консоль браузера (F12) и посмотрите логи!










